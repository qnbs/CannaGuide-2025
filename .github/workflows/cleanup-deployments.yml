name: Cleanup Old Deployments

on:
    schedule:
        # Run weekly on Sunday at 03:00 UTC
        - cron: '0 3 * * 0'
    workflow_dispatch:
        inputs:
            keep_count:
                description: 'Number of recent deployments to keep per environment'
                required: false
                default: '3'

permissions: {}

jobs:
    cleanup:
        name: Prune old deployments
        runs-on: ubuntu-latest
        timeout-minutes: 10
        permissions:
            contents: read
        steps:
            - name: Harden runner
              uses: step-security/harden-runner@ab7a9404c0f3da075243ca237b5fac12c98deaa5 # v2.19.3
              with:
                  egress-policy: audit

            - name: Report old deployments (read-only)
              uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9.0.0
              with:
                  script: |
                      const keep = parseInt('${{ inputs.keep_count }}' || '3', 10);
                      const owner = context.repo.owner;
                      const repo = context.repo.repo;

                      // Get all deployments (paginated)
                      const deployments = await github.paginate(
                        github.rest.repos.listDeployments,
                        { owner, repo, per_page: 100 }
                      );

                      // Group by environment
                      const byEnv = {};
                      for (const d of deployments) {
                        const env = d.environment || 'unknown';
                        if (!byEnv[env]) byEnv[env] = [];
                        byEnv[env].push(d);
                      }

                      let candidates = 0;
                      for (const [env, deps] of Object.entries(byEnv)) {
                        // Sort newest first
                        deps.sort((a, b) => new Date(b.created_at) - new Date(a.created_at));
                        const toDelete = deps.slice(keep);

                        if (toDelete.length === 0) {
                          core.info(`[${env}] ${deps.length} deployments, nothing to prune`);
                          continue;
                        }

                        core.info(`[${env}] ${deps.length} total, keeping ${keep}, candidates ${toDelete.length}`);

                        for (const d of toDelete) {
                          try {
                            candidates++;
                          } catch (err) {
                            core.warning(`Failed to inspect deployment ${d.id} (${env}): ${err.message}`);
                          }
                        }
                      }

                      core.info(`[OK] Found ${candidates} old deployment candidate(s)`);
