#!/bin/sh
set -e

# The Node runner serializes hooks, refuses implicit dependency installation,
# and runs lint-staged groups one at a time on low-resource machines.
exec node ./scripts/git-hooks/run-hook.mjs pre-commit
