#!/bin/sh
set -e

# Fast pre-push gate: identity check only.
# Lint + typecheck + tests run in CI (ci.yml quality/security/e2e jobs).
# Pre-commit hook already covers lint-staged + trojan-source + prettier.
pnpm run check:commit-identity
