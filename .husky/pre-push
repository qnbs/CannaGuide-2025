#!/bin/sh
set -e

# The Node runner holds the single-hook lock through every step, checks memory
# and dependency synchronization, emits heartbeats, and terminates timed-out
# child process groups. Exhaustive tests/builds/security checks remain in CI.
exec node ./scripts/git-hooks/run-hook.mjs pre-push "$@"
