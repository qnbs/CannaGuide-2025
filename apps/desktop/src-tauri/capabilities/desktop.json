{
    "$schema": "../gen/schemas/desktop-schema.json",
    "identifier": "desktop",
    "description": "Desktop-specific capabilities: opening external links via the shell plugin",
    "windows": ["main"],
    "platforms": ["linux", "macOS", "windows"],
    "permissions": ["shell:default", "shell:allow-open"]
}
