import React from 'react';

/**
 * This component is considered obsolete after the migration from Zustand to Redux.
 * The simulation loop is now handled by `simulationLoop.ts` and the `SimulationController` component.
 * This file is kept to avoid breaking file structures but is functionally empty.
 */
export const SimulationManager: React.FC = () => {
    return null;
};
