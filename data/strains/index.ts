import type { Strain } from '@/types';

// This file is intentionally left empty.
// The strain data is now loaded dynamically from static JSON files
// via the strainService to improve initial app load performance.
export const allStrainsData: Strain[] = [];
